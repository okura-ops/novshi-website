---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import CTASection from '../components/CTASection.astro';
import { AVAILABLE, RENTALS_ORDERED, OCCUPIED } from '../data/rentals';
import SoldStamp from '../components/SoldStamp.astro';

const recruitItems = [
  { title: '採用戦略の設計', desc: '誰を、いつまでに、いくらで採るかを数字で決めます。' },
  { title: '求人原稿の作成', desc: '入社後を想像できる粒度で仕事と待遇を書きます。' },
  { title: '媒体運用', desc: 'Indeed・Meta広告・採用HPを応募単価で配分します。' },
  { title: '面接の設計と同席', desc: '評価項目を先に揃えます。同席もできます。' },
  { title: '内定から入社まで', desc: '辞退が出やすい局面で先に動きます。' },
  { title: '定着支援', desc: '入社後1・3・6か月を目安に面談します。' },
];
const channels = [
  { num: '01', label: 'Indeed', desc: '仕事を探す人の検索結果に載る、応募獲得の中心媒体です。' },
  { num: '02', label: 'Meta広告', desc: '転職を考えはじめる前の層に、求人を先に見せます。' },
  { num: '03', label: '自社採用HP', desc: '社名で検索した応募者の疑問に、一次情報で答える場所です。' },
];
const globalItems = [
  { title: '人材紹介', desc: '職種要件との適合を確認してから紹介します。' },
  { title: '住まいの確保', desc: '社宅として貸せる住居を自社で用意します。空き家再生事業が受け皿です。' },
  { title: '定着フォロー', desc: '入社後の生活相談を受け、気になる変化を企業へ共有します。' },
];
const kaizenItems = [
  { title: '組織づくり', desc: '評価・役割分担・会議体など、人が動く仕組みを整えます。' },
  { title: '業務の仕組み化', desc: '属人化した業務を棚卸しし、手順化からAI活用まで進めます。' },
  { title: '経営の数値管理', desc: '売上・採用・定着の指標を設計し、月次で回る形にします。' },
  { title: '実行支援', desc: '計画づくりで終わらせず、現場での実行まで一緒に担います。' },
];
const troubles = [
  '応募対応が兼任で後回しになり、辞退が続いている',
  '担当者の退職で、採用がそのまま止まっている',
  '媒体に出しても応募が来ず、どこから直すべきか分からない',
  '採用できても住まいが決まらず、入社に至らない（外国人採用）',
];
const steps = [
  { num: '1', title: 'ヒアリング', desc: '採用と組織の現状を伺います。相談は無料です。' },
  { num: '2', title: '支援範囲の設計', desc: '何をどこまで任せるかを文書で確定します。' },
  { num: '3', title: '運用開始', desc: '実務を開始し、月次で数字と論点を報告します。' },
];
// 法人向け社宅（1棟借り上げ）。配布中のチラシ（03_clients/kcs）と同じ事実で揃える。
// 賃料の具体額はチラシ側にのみ記載する（紹介経由の企業さま向けの条件のため、公開ページには出さない）
const shatakuPairs = [
  ['実習生が入れ替わるたび、空室の家賃と原状回復の手配が残る', '清掃・原状回復まで込み', '1棟契約に含みます。空室期間の扱いも都度ご相談できます。'],
  ['手取りが増えた社員が、1人で住める部屋を見つけられない', '単身で住める戸建を用意', '人数と予算に合わせて、単身用から複数名用まで組みます。'],
  ['次の物件が見つかる保証がなく、使わない部屋の契約を続けている', '必要な棟数を、必要な期間だけ', '増やす・減らすはそのつどご相談いただけます。'],
  ['入居のたびに敷金・礼金と家具の費用がかかる', '敷金・礼金なし、備品つき', '食器や調理器具を置いた状態でお渡しします。'],
  ['早期に辞めてしまう。体調やメンタルの面倒まで見きれない', '母国の暮らしに合わせて直せる', '1棟まるごとなので、台所や水まわりを変えられます。'],
];
const shatakuWhy = [
  { num: '01', label: '自分たちで買って直す', desc: '仲介や転貸を挟みません。あいだで乗る手数料がないぶん、この家賃で出せます。' },
  { num: '02', label: 'まとめて発注する', desc: '熊本・福岡で100件を超える空き家を直してきました。同じ業者に数をまとめて出すので、1戸あたりの工事費が下がります。' },
  { num: '03', label: '1棟を1社に貸す', desc: '働く方が個別に大家と契約しません。保証人を理由に断られることも、窓口が分かれることもありません。' },
];
// 実績帯。検証できる数字だけを置く（Manus競合調査 §5-1。100件超は対外承認済みの表現、
// 募集中の棟数は下の物件一覧と同じRENTALSから採るのでズレない）
const shatakuStats = [
  { num: '100', unit: '件超', label: '熊本・福岡で手がけた空き家再生' },
  { num: String(AVAILABLE.length), unit: '棟', label: 'いま募集している自社保有の戸建' },
  { num: '0', unit: '円', label: '敷金・礼金・仲介手数料' },
];
// 役割の図解。誰が何を持つかを文章でなく面で見せる（同 §5-2）
const shatakuRoles = [
  { who: '御社', desc: '賃料のお支払いと、どなたが住むかの決定。物件探し・大家との交渉・入退去の手配は発生しません。' },
  { who: 'novshi', desc: '空き家の仕入れと改修、1棟の貸出、備品の設置、退去後の清掃と原状回復。窓口はここ1つです。' },
  { who: '働く方', desc: '住むだけ。個別に大家と契約しないので、保証人を理由に断られることがありません。' },
];
// 室内の実写。外観だけでは「生活できる状態か」が伝わらない（Manus競合調査 §5-4）。
// altは src/data/rentals.ts の photos に合わせる。改修前後の比較はできないので「改修後」とは書かない
const shatakuShots = [
  { src: '/images/rent/006-ikisu/03.webp', alt: '飯塚市伊岐須の戸建の対面キッチン', cap: '対面キッチン' },
  { src: '/images/rent/006-ikisu/05.webp', alt: '飯塚市伊岐須の戸建の2階洋室（2面採光）', cap: '2階の洋室（2面採光）' },
  { src: '/images/rent/006-ikisu/06.webp', alt: '飯塚市伊岐須の戸建の浴室', cap: '浴室' },
];
const shatakuTerms = [
  ['対象', '技能実習生・特定技能1号／2号・留学生など、外国籍の従業員の方'],
  ['契約形態', '御社と当社の1棟借り上げ契約（契約は1本）'],
  ['初期費用・備品', '敷金・礼金・仲介手数料なしが基本。食器や調理器具は相談のうえ設置します'],
  ['入替・退去時', '清掃と原状回復は当社で手配。空室期間の扱いは個別にご相談ください'],
  ['居室の基準', '技能実習は寝室4.5㎡以上／2名以下、特定技能1号は居室7.5㎡以上に対応します'],
  ['賃料', '物件と人数によって異なります。条件を伺ったうえでお見積りします'],
];
---

<Layout title="サービス | 株式会社novshi" description="採用支援・組織改善と、空き家を社宅に変える事業。九州の中小企業の採用と定着を、代表が直接支えます。">
  {/* TODO:COPY services-hero */}
  <Hero label="Services" title="novshiの事業" subtitle="九州の中小企業の採用と組織を支える仕事と、空き家を働く人の社宅に変える仕事をしています。" compact={true} />

  <!-- サービスタブ: 採用・経営支援 ／ 空き家再生 -->
  <div class="bg-white border-t border-gray-200 sticky top-16 lg:top-[72px] z-30" id="service-tabs">
    <div class="max-w-[1120px] mx-auto px-5 sm:px-8">
      <div class="flex gap-10">
        <button type="button" data-tab="shien" class="tab-btn cursor-pointer relative py-5 text-base lg:text-lg font-bold text-gray-900 transition-colors duration-200 hover:text-gray-900" aria-selected="true">
          採用・経営支援
          <img src="/images/brush/line-sumi.png" alt="" aria-hidden="true" width="1118" height="103" class="tab-ink absolute bottom-0 left-0 w-full h-1.5 object-cover select-none" />
        </button>
        <button type="button" data-tab="akiya" class="tab-btn cursor-pointer relative py-5 text-base lg:text-lg font-bold text-text-muted transition-colors duration-200 hover:text-gray-900" aria-selected="false">
          空き家再生
          <img src="/images/brush/line-sumi.png" alt="" aria-hidden="true" width="1118" height="103" class="tab-ink absolute bottom-0 left-0 w-full h-1.5 object-cover select-none hidden" />
        </button>
      </div>
    </div>
  </div>



  <div data-panel="shien">
  <!-- 柱1: 採用支援 -->
  <section id="recruitment" class="sec-xl scroll-mt-32 bg-white border-t border-gray-100">
    <div class="max-w-[1120px] mx-auto px-5 sm:px-8">
      <div class="max-w-2xl fade-in-up">
        {/* TODO:COPY services-recruit-head */}
        <p class="font-num text-5xl lg:text-6xl font-medium text-gray-200 leading-none mb-5" aria-hidden="true">01</p>
        <h2 class="text-2xl lg:text-3xl font-bold text-gray-900 mb-6">採用支援</h2>
        <p class="text-lg font-bold text-gray-900 mb-4">「もう一人の経営者。」</p>
        {/* TODO:COPY services-recruit-lead */}
        <p class="text-gray-700 leading-[1.9]">
          採用担当がいない会社の、採用担当になります。実務代行ではなく、経営者の隣で採否を一緒に決める立場です。
        </p>
      </div>

      <!-- 戦略の階層: 採用戦略は事業戦略→組織戦略の順でしか立てられない -->
      <div class="mt-14 grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
        <div class="lg:col-span-5 fade-in-up">
          <h3 class="text-xl lg:text-2xl font-bold text-gray-900 mb-5">採用は、経営から逆算する</h3>
          <p class="text-sm text-gray-700 leading-[1.9] max-w-md">
            採用戦略は、単独では立てられません。事業戦略が決まり、それを実行する組織のかたちが決まって、はじめて「誰を、いつ採るか」が決まります。この順番を飛ばした求人は、応募が来ても定着しません。
          </p>
          <p class="mt-4 text-sm text-gray-700 leading-[1.9] max-w-md">
            だからnovshiの採用支援は、経営・事業のレイヤーに立てるメンバーのみで行います。求人の書き方ではなく、事業の勝ち筋から採用を設計します。
          </p>
        </div>
        <div class="lg:col-span-7 fade-in-up" role="img" aria-label="事業戦略、組織戦略、採用戦略の順に決まる階層図。novshiは3層すべてを支援範囲とする">
          <div class="max-w-xl mx-auto">
            <div class="flex items-stretch gap-5">
              <div class="flex-1">
                <div class="bg-[#1A1A1A] px-6 py-5 lg:px-8">
                  <p class="text-[11px] text-white/50 mb-1"><span class="font-num">1</span></p>
                  <p class="text-base lg:text-lg font-bold text-white">事業戦略</p>
                  <p class="mt-1 text-xs text-white/65">どの市場で、何で勝つか</p>
                </div>
                <div class="bg-[#4D4D4D] px-6 py-5 lg:px-8 w-[86%]">
                  <p class="text-[11px] text-white/50 mb-1"><span class="font-num">2</span></p>
                  <p class="text-base lg:text-lg font-bold text-white">組織戦略</p>
                  <p class="mt-1 text-xs text-white/70">どんな組織と役割で実行するか</p>
                </div>
                <div class="bg-[#8A8A8A] px-6 py-5 lg:px-8 w-[72%]">
                  <p class="text-[11px] text-white/60 mb-1"><span class="font-num">3</span></p>
                  <p class="text-base lg:text-lg font-bold text-white">採用戦略</p>
                  <p class="mt-1 text-xs text-white/75">誰を、いつ、どう採るか</p>
                </div>
              </div>
              <div class="flex flex-col items-center shrink-0 pt-1 pb-1">
                <img src="/images/brush/line-sumi.png" alt="" aria-hidden="true" class="h-full w-2 object-cover select-none" style="object-position: center;" />
              </div>
              <div class="flex items-center shrink-0">
                <p class="text-sm font-bold text-gray-900" style="writing-mode: vertical-rl; letter-spacing: 0.18em;">novshiの支援範囲</p>
              </div>
            </div>
            <p class="mt-4 text-sm text-gray-500">採用戦略は、この順番でしか立てられません。</p>
          </div>
        </div>
      </div>

      <div class="max-w-2xl">
        {/* 課題の言語化（RPOサービスページの定石: 悩みを先に置く） */}
        <div class="mt-10">
          <p class="text-sm font-bold text-gray-900 mb-4">こんな状態の会社を支えています</p>
          <ul class="space-y-2.5 text-sm text-gray-700 leading-[1.8]">
            {troubles.map((t) => (
              <li class="pl-5 relative before:content-['—'] before:absolute before:left-0 before:text-gray-400">{t}</li>
            ))}
          </ul>
        </div>
      </div>

      <div class="mt-14 grid grid-cols-1 md:grid-cols-2 gap-x-12 stagger">
        {recruitItems.map((item) => (
          <div class="border-t border-gray-200 py-6">
            <h3 class="text-sm font-bold text-gray-900 mb-1">{item.title}</h3>
            <p class="text-sm text-gray-700 leading-[1.9]">{item.desc}</p>
          </div>
        ))}
      </div>

      <!-- 役割分担の対比（委託できる範囲を先に見せる） -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        <h3 class="text-xl font-bold text-gray-900 mb-8 fade-in-up">役割分担</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 max-w-4xl fade-in-up">
          <div class="bg-[#1A1A1A] p-8 lg:p-9">
            <p class="text-sm font-bold text-white mb-5">novshiが担うこと</p>
            <ul class="space-y-3 text-sm text-white/80 leading-[1.8]">
              <li>採用戦略の設計と数値目標づくり</li>
              <li>求人原稿の作成・改稿とA/Bテスト</li>
              <li>媒体運用と応募単価の管理</li>
              <li>面接の設計、応募者への連絡</li>
              <li>内定後のフォローと定着面談</li>
            </ul>
          </div>
          <div class="bg-[#F2F2F2] p-8 lg:p-9">
            <p class="text-sm font-bold text-gray-900 mb-5">お客様にお願いすること</p>
            <ul class="space-y-3 text-sm text-gray-700 leading-[1.8]">
              <li>採否の最終決定</li>
              <li>労働条件・待遇の決定</li>
              <li>面接の実施（novshiの同席は可能です）</li>
            </ul>
          </div>
        </div>
      </div>

      <!-- 運用する3つのチャネル: 均等3カラムカードをやめて行リストに -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        {/* TODO:COPY services-recruit-channels-head */}
        <h3 class="text-xl font-bold text-gray-900 mb-2 fade-in-up">運用する3つのチャネル</h3>
        <p class="text-gray-700 mb-8 fade-in-up">媒体ごとに役割を分けて使います。</p>
        <div class="stagger max-w-3xl">
          {channels.map((ch) => (
            <div class="grid grid-cols-[3.5rem_10rem_1fr] max-sm:grid-cols-[3.5rem_1fr] items-baseline gap-3 py-5 border-b border-gray-200">
              <span class="font-num text-2xl font-medium text-gray-300">{ch.num}</span>
              <p class="text-base font-bold text-gray-900">{ch.label}</p>
              <p class="text-sm text-gray-700 leading-[1.9] max-sm:col-span-2 max-sm:pl-14">{ch.desc}</p>
            </div>
          ))}
        </div>
      </div>

      <!-- 進め方: 3ステップ（複雑さを見せない） -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        <h3 class="text-xl font-bold text-gray-900 mb-8 fade-in-up">ご依頼から開始まで</h3>
        <div class="flex flex-col md:flex-row md:items-stretch gap-y-6 max-w-4xl stagger">
          {steps.map((s, i) => (
            <>
              {i > 0 && <div class="hidden md:flex items-center px-4 text-gray-300 text-xl" aria-hidden="true">→</div>}
              <div class="flex-1 pt-5">
                <img src="/images/brush/line-sumi.png" alt="" aria-hidden="true" width="1118" height="103" class="brush-rule h-2 mb-5 select-none" />
                <p class="font-num text-2xl font-medium text-gray-300 leading-none mb-3">{s.num}</p>
                <p class="text-base font-bold text-gray-900">{s.title}</p>
                <p class="mt-2 text-sm text-gray-700 leading-[1.8]">{s.desc}</p>
              </div>
            </>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- 外国人採用・定着支援 -->
  <section class="sec-lg bg-bg-secondary">
    <div class="max-w-[1120px] mx-auto px-5 sm:px-8">
      <div class="max-w-3xl fade-in-up">
        <h3 class="text-xl font-bold text-gray-900 mb-4">外国人採用・定着支援</h3>
        {/* TODO:COPY services-global-lead */}
        <p class="text-gray-700 leading-[1.9] mb-8 max-w-xl">
          外国人採用でよくあるつまずきは、内定後に住まいが決まらないことです。novshiは人材紹介（有料職業紹介事業許可 40-ユ-301708）に、社宅の用意を組み合わせています。
        </p>
        <div class="stagger">
          {globalItems.map((item) => (
            <div class="border-t border-gray-300/70 py-5">
              <h4 class="text-sm font-bold text-gray-900 mb-1">{item.title}</h4>
              <p class="text-sm text-gray-700 leading-[1.9] max-w-lg">{item.desc}</p>
            </div>
          ))}
        </div>
        <div class="mt-8 flex flex-wrap gap-3">
          <a href="/contact" class="inline-flex items-center justify-center px-6 py-3 text-sm font-semibold text-white bg-[#1A1A1A] rounded-none hover:opacity-85 transition-opacity duration-200">相談する</a>
          <a href="/contact?type=doc" class="inline-flex items-center justify-center px-6 py-3 text-sm font-semibold text-gray-900 border border-gray-900 rounded-none hover:bg-[#F2F2F2] transition-colors duration-200">資料を請求する</a>
        </div>
      </div>
    </div>
  </section>

  <!-- 柱2: 組織・事業・経営の改善支援 -->
  <section id="consulting" class="sec-lg bg-white scroll-mt-20">
    <div class="max-w-[1120px] mx-auto px-5 sm:px-8">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
        <div class="lg:col-span-7 fade-in-up">
          {/* TODO:COPY services-kaizen-head */}
          <p class="font-num text-5xl lg:text-6xl font-medium text-gray-200 leading-none mb-5" aria-hidden="true">02</p>
          <h2 class="text-2xl lg:text-3xl font-bold text-gray-900 mb-6">組織・事業・経営の改善支援</h2>
          {/* TODO:COPY services-kaizen-lead */}
          <p class="text-gray-700 leading-[1.9] max-w-xl">
            人を起点に、組織・事業・経営の課題を整理し、改善の実行まで担います。入り口はほとんどの場合、採用です。人が採れない理由を追いかけると、評価の仕組みや業務の回し方、投資判断の順序に行き着きます。novshiはそこまで一緒に踏み込みます。
          </p>
        </div>
        <div class="lg:col-span-5 fade-in-up">
          <!-- 同心円図: 人を起点に組織・事業・経営へ広がる -->
          <!-- 同心円: 購入した筆致円相（実スキャン由来）3輪で構成（Web DS v1.1 §4-2） -->
          <svg viewBox="0 0 520 470" class="w-full max-w-sm mx-auto" role="img" aria-label="中心の「人」を、組織・事業・経営の3つの筆致の円が囲む図">
            <image href="/images/brush/enso-3-g300.png" x="35" y="35" width="450" height="425" />
            <image href="/images/brush/enso-5-g500.png" x="105" y="105" width="310" height="288" />
            <image href="/images/brush/enso-1.png" x="172" y="170" width="176" height="166" />
            <circle cx="260" cy="250" r="42" fill="#1A1A1A" />
            <text x="260" y="259" font-size="22" font-weight="bold" fill="#fff" text-anchor="middle">人</text>
          </svg>
          <div class="mt-6 max-w-sm mx-auto space-y-2.5">
            <p class="text-sm leading-[1.8]"><span class="font-bold text-gray-900">● 組織</span><span class="text-gray-700">　評価・役割・定着</span></p>
            <p class="text-sm leading-[1.8]"><span class="font-bold" style="color:#8A8A8A">● 事業</span><span class="text-gray-700">　業務・営業・仕組み化</span></p>
            <p class="text-sm leading-[1.8]"><span class="font-bold" style="color:#C0C0C0">● 経営</span><span class="text-gray-700">　数値管理・投資判断</span></p>
          </div>
          
        </div>
      </div>

      <div class="mt-12 stagger max-w-3xl">
        {kaizenItems.map((item) => (
          <div class="border-t border-gray-200 py-6">
            <h3 class="text-sm font-bold text-gray-900 mb-1">{item.title}</h3>
            <p class="text-sm text-gray-700 leading-[1.9]">{item.desc}</p>
          </div>
        ))}
      </div>

      <!-- 支援の実例（守秘のため業種のみ） -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        <h3 class="text-xl font-bold text-gray-900 mb-2 fade-in-up">支援の実例</h3>
        <p class="text-sm text-text-muted mb-8 fade-in-up">守秘のため、社名を伏せて業種のみ記載しています。</p>
        <div class="stagger max-w-3xl">
          <div class="border-t border-gray-200 py-6">
            <p class="text-sm font-bold text-gray-900 mb-1">不動産会社 — 採用から、人事制度の設計へ</p>
            <p class="text-sm text-gray-700 leading-[1.9]">採用支援として始まり、応募者管理の仕組み（通知の自動化・管理シート）と人事制度の設計まで担当しています。</p>
          </div>
          <div class="border-t border-gray-200 py-6">
            <p class="text-sm font-bold text-gray-900 mb-1">求人メディア運営会社 — 営業部門の立て直しへ</p>
            <p class="text-sm text-gray-700 leading-[1.9]">支社の営業立て直しプロジェクトに外部アドバイザーとして参画。営業データの整備と、月次で数字を追う会議体を設計しています。</p>
          </div>
          <div class="border-t border-gray-200 py-6">
            <p class="text-sm font-bold text-gray-900 mb-1">ITコンサルティング会社 — 採用投資の意思決定へ</p>
            <p class="text-sm text-gray-700 leading-[1.9]">高額な採用媒体に出稿する前に、無料チャネルで応募の集まり方を検証する設計に変更。数字が出てから投資を判断する形にしました。</p>
          </div>
        </div>
      </div>

      {/* TODO:COPY services-kaizen-price-note */}
      <p class="mt-10 text-sm text-text-muted leading-relaxed">
        内容と範囲を確認したうえで、個別に見積もります。
      </p>
    </div>
  </section>

  </div>

  <div data-panel="akiya" class="hidden">
  <!-- 空き家再生×社宅 -->
  <section id="akiya" class="sec-xl scroll-mt-32 bg-white">
    <div class="max-w-[1120px] mx-auto px-5 sm:px-8">
      <div class="max-w-2xl fade-in-up">
        {/* TODO:COPY services-akiya-head */}
        <h2 class="text-2xl lg:text-3xl font-bold text-gray-900 mb-6">空き家再生×社宅</h2>
        {/* TODO:COPY services-akiya-lead */}
        <p class="text-gray-700 leading-[1.9]">
          空き家を買い取り、改修し、介護・建設・飲食の現場で働く人や外国人材の社宅として長期で貸します。物件の情報は、採用支援で付き合いのある地場企業から届きます。社宅は採用した人材の定着に使われます。
        </p>
      </div>

      <!-- 実績帯: 説明より先に検証できる数字を置く（Manus競合調査 §5-1／群Cで5/6サイトが採る型） -->
      <div class="mt-12 bg-[#F2F2F2] px-7 py-9 sm:px-10">
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-y-8 stagger">
          {shatakuStats.map((s, i) => (
            <div class:list={['sm:px-8', i === 0 && 'sm:pl-0', i === 2 && 'sm:pr-0',
                              i > 0 && 'sm:border-l sm:border-gray-900/15']}>
              <span class="block w-10 h-[3px] bg-[#B5502A] mb-4" aria-hidden="true"></span>
              <p class="leading-none">
                <span class="font-num text-5xl font-medium text-gray-900">{s.num}</span>
                <span class="text-lg font-bold text-gray-900 ml-1">{s.unit}</span>
              </p>
              <p class="mt-3 text-sm text-gray-700 leading-[1.8]">{s.label}</p>
            </div>
          ))}
        </div>
      </div>

      <!-- 法人向け: 外国人材の社宅（1棟借り上げ）。配布中のチラシと同じ内容を公開ページ版に落としたもの -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        <!-- 導入: テキストだけで入らず、写真を面として置く（AI感規範 §4-4-1） -->
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-x-12 gap-y-8 items-center">
          <div class="lg:col-span-6 fade-in-up">
            <h3 class="text-xl lg:text-[22px] font-bold text-gray-900 mb-4">外国人材の社宅を、戸建1棟でお貸しします</h3>
            <p class="text-base lg:text-lg text-gray-700 leading-[1.9]">
              技能実習生・特定技能人材を受け入れる企業さま向けに、再生した戸建を1棟まるごとお貸ししています。契約は御社と当社の1本だけ。入退去のたびの清掃と原状回復、生活に要る備品の用意まで、こちらで段取りします。
            </p>
          </div>
          <div class="lg:col-span-6 fade-in-up">
            <img src="/images/rent/008-hirowatari/01.webp" alt="当社が保有する戸建の外観" class="w-full aspect-[4/3] object-cover" loading="lazy" width="800" height="600" />
          </div>
        </div>

        <!-- 役割の図解: 誰が何を持つかを文章でなく面で示す（Manus競合調査 §5-2／群Aで4/7サイトが図解） -->
        <div class="mt-14">
          <h4 class="text-xl font-bold text-gray-900 mb-8 fade-in-up">誰が何を持つか</h4>
          <div class="flex flex-col md:flex-row md:items-stretch gap-y-6 stagger">
            {shatakuRoles.map((r, i) => (
              <>
                {i > 0 && <div class="hidden md:flex items-center px-4 text-gray-300 text-xl" aria-hidden="true">→</div>}
                <div class="flex-1 pt-5 border-t border-gray-900">
                  <p class="text-lg font-bold text-gray-900">{r.who}</p>
                  <p class="mt-3 text-base text-gray-700 leading-[1.9]">{r.desc}</p>
                </div>
              </>
            ))}
          </div>
        </div>

        <!-- 困りごと → novshiの社宅なら -->
        <div class="mt-16 max-w-4xl">
          <div class="hidden md:grid grid-cols-[1fr_1.2fr] gap-x-10 pb-3 border-b border-gray-900 fade-in-up">
            <p class="text-sm font-bold text-text-muted">よくある困りごと</p>
            <p class="text-sm font-bold text-gray-900">novshiの社宅なら</p>
          </div>
          <div class="stagger">
            {shatakuPairs.map(([trouble, key, detail]) => (
              <div class="grid grid-cols-1 md:grid-cols-[1fr_1.2fr] gap-x-10 gap-y-2 py-7 border-b border-gray-200">
                <p class="text-base text-gray-700 leading-[1.9]">{trouble}</p>
                <div>
                  <p class="text-lg font-bold text-gray-900 leading-[1.6]">{key}</p>
                  <p class="mt-2 text-base text-gray-700 leading-[1.9]">{detail}</p>
                </div>
              </div>
            ))}
          </div>
        </div>

        <!-- なぜこの条件で貸せるのか: 行リストが3連続しないよう黒面に移す（AI感規範 §4-4-2） -->
        <div class="mt-16 bg-[#1A1A1A] px-7 py-10 sm:px-10 lg:px-12 lg:py-12">
          <h4 class="text-xl font-bold text-white mb-2 fade-in-up">この条件で貸せる理由</h4>
          <p class="text-base text-white/70 mb-10 fade-in-up">仕入れから改修まで自社で持っているので、家賃を抑えられます。</p>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-x-10 gap-y-9 stagger">
            {shatakuWhy.map((w) => (
              <div>
                <p class="font-num text-4xl font-medium text-white/25 leading-none mb-4">{w.num}</p>
                <p class="text-lg font-bold text-white leading-[1.5]">{w.label}</p>
                <p class="mt-3 text-base text-white/70 leading-[1.9]">{w.desc}</p>
              </div>
            ))}
          </div>
        </div>

        <!-- 条件 -->
        <div class="mt-16">
          <h4 class="text-xl font-bold text-gray-900 mb-8 fade-in-up">お貸しする条件</h4>
          {/* 社内共有・監理団体との確認に引用できるよう、行リストではなく表にする（Manus競合調査 §5-3） */}
          <div class="max-w-4xl fade-in-up">
            <table class="w-full border-collapse text-left">
              <thead>
                <tr class="bg-[#1A1A1A] text-white">
                  <th scope="col" class="w-[28%] px-5 py-4 text-sm font-bold">項目</th>
                  <th scope="col" class="px-5 py-4 text-sm font-bold">内容</th>
                </tr>
              </thead>
              <tbody>
                {shatakuTerms.map(([term, desc]) => (
                  <tr class="border-b border-[#D9D9D9]">
                    <th scope="row" class="px-5 py-5 align-top text-base font-bold text-gray-900">{term}</th>
                    <td class="px-5 py-5 text-base text-gray-700 leading-[1.7]">{desc}</td>
                  </tr>
                ))}
              </tbody>
            </table>
          </div>
        </div>

        <!-- エリア: 在庫から選ばせる形ではないことを明示する -->
        <div class="mt-12 bg-[#F2F2F2] p-8 lg:p-9 max-w-3xl fade-in-up">
          <p class="text-lg font-bold text-gray-900">掲載の物件になくても、ご希望のエリアで仕入れます。</p>
          <p class="mt-2.5 text-base text-gray-700 leading-[1.9]">
            いま持っている物件から選んでいただく形ではありません。勤務地と条件を伺ってから、その場所で空き家を探して直します。対象エリアは気にせずご相談ください。登録支援機関・監理団体さまを通じたご紹介も承っています。
          </p>
          <div class="mt-5 flex flex-wrap gap-3">
            <a href="/contact" class="inline-flex items-center justify-center px-6 py-3 text-sm font-semibold text-white bg-[#1A1A1A] rounded-none hover:opacity-85 transition-opacity duration-200">社宅について相談する</a>
            <a href="/contact?type=doc" class="inline-flex items-center justify-center px-6 py-3 text-sm font-semibold text-gray-900 border border-gray-900 rounded-none hover:bg-white transition-colors duration-200">資料を請求する</a>
          </div>
        </div>
      </div>

      <!-- 室内の実写: 外観だけでは生活できる状態か伝わらない（Manus競合調査 §5-4） -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        <h3 class="text-xl lg:text-[22px] font-bold text-gray-900 mb-4 fade-in-up">お渡しするのは、この状態です</h3>
        <p class="text-base text-gray-700 leading-[1.9] max-w-3xl mb-8 fade-in-up">
          水まわりと居室は、入居した日から使える状態にしてお渡しします。食器や調理器具など生活に要るものも、相談のうえ置いておきます。
        </p>
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-x-6 gap-y-8 stagger">
          {shatakuShots.map((s) => (
            <figure>
              <img src={s.src} alt={s.alt} class="w-full aspect-[4/3] object-cover" loading="lazy" width="800" height="600" />
              <figcaption class="mt-3 text-sm text-gray-500">{s.cap}</figcaption>
            </figure>
          ))}
        </div>
      </div>

      <!-- 物件一覧: 募集中は /rent/ の詳細ページへリンク（データの正本=src/data/rentals.ts、その上流=Notion取得物件管理DBとジモティー掲載） -->
      <div class="mt-16 pt-12 border-t border-gray-200">
        <h3 class="text-xl font-bold text-gray-900 mb-8 fade-in-up">物件情報</h3>
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-x-6 gap-y-10 stagger">
          {RENTALS_ORDERED.map((r) => (
            <a href={`/rent/${r.slug}`} class="flex flex-col group">
              <div class="relative overflow-hidden">
                <img src={`/images/rent/${r.slug}/01.webp`} alt={r.photos[0].alt} class="w-full aspect-[4/3] object-cover transition-transform duration-300 group-hover:scale-[1.03]" loading="lazy" width="800" height="600" />
                {r.closed && <SoldStamp compact={true} />}
                <span class:list={['absolute top-0 left-0 z-20 text-[11px] font-semibold px-3 py-1.5 text-white', r.closed ? 'bg-[#B5502A]' : 'bg-[#1A1A1A]']}>
                  {r.closed ? r.closed.label : '募集中'}
                </span>
              </div>
              <div class="pt-3">
                <div class="flex items-baseline justify-between gap-3">
                  <p class="text-base font-bold text-gray-900">{r.name}</p>
                  {r.closed ? (
                    <p class="text-sm text-gray-500">{r.closed.since}〜</p>
                  ) : (
                    <p class="text-base text-gray-900"><span class="font-num font-semibold">{(r.rent / 10000).toLocaleString('ja-JP')}</span><span class="text-sm font-medium">万円／月</span></p>
                  )}
                </div>
                <p class="mt-1.5 text-sm text-gray-500">{r.spec}</p>
              </div>
            </a>
          ))}
          {OCCUPIED.map((p) => (
            <div class="flex flex-col">
              <div class="relative w-full aspect-[4/3] bg-[#1A1A1A] overflow-hidden">
                <img aria-hidden="true" src="/images/site/mark-enso-white.png" alt="" width="888" height="981" class="absolute -right-[16%] -bottom-[40%] w-[240px] max-w-none opacity-[0.12] select-none pointer-events-none" loading="lazy" />
                <span class="absolute top-0 left-0 text-[11px] font-semibold px-3 py-1.5 bg-white text-gray-900">入居中</span>
              </div>
              <div class="pt-3 flex items-baseline justify-between gap-3">
                <p class="text-base font-bold text-gray-900">{p.name}</p>
                <p class="text-sm text-gray-500">{p.spec}</p>
              </div>
            </div>
          ))}
        </div>
        <p class="mt-5 text-sm text-gray-500 fade-in-up">物件名をタップすると、賃料・入居条件・写真の詳細ページが開きます（<a href="/rent" class="underline underline-offset-4 hover:text-gray-900">入居者募集の物件一覧</a>）。</p>
      </div>

      <div class="mt-12 bg-[#F2F2F2] p-7 max-w-xl fade-in-up">
        <p class="text-sm font-bold text-gray-900 mb-2">入居をお考えの個人の方へ</p>
        <p class="text-sm text-gray-700 leading-[1.9]">
          募集中の物件の内覧予約・空き状況は、LINEまたはお問い合わせフォームからご連絡ください。
        </p>
        <div class="mt-4 flex flex-wrap gap-3">
          <a href="https://lin.ee/Ylj7svr" target="_blank" rel="noopener" class="inline-flex items-center justify-center px-5 py-2.5 text-sm font-semibold text-white bg-[#06C755] rounded-none hover:opacity-85 transition-opacity duration-200">LINEで相談する</a>
          <a href="/contact" class="inline-flex items-center justify-center px-5 py-2.5 text-sm font-semibold text-gray-900 border border-gray-900 rounded-none hover:bg-[#F2F2F2] transition-colors duration-200">フォームで問い合わせる</a>
        </div>
      </div>

    </div>
  </section>
  </div>



  <CTASection />

  <script>
    // サービスタブ切替（#recruitment/#consulting=採用・経営支援、#akiya=空き家再生）
    const btns = document.querySelectorAll('.tab-btn');
    const panels: Record<string, HTMLElement | null> = {
      shien: document.querySelector('[data-panel="shien"]'),
      akiya: document.querySelector('[data-panel="akiya"]'),
    };
    function select(tab: string, scrollTo?: string) {
      btns.forEach((b) => {
        const on = (b as HTMLElement).dataset.tab === tab;
        b.setAttribute('aria-selected', String(on));
        b.classList.toggle('text-gray-900', on);
        b.classList.toggle('text-text-muted', !on);
        b.querySelector('.tab-ink')!.classList.toggle('hidden', !on);
      });
      Object.entries(panels).forEach(([k, el]) => el && el.classList.toggle('hidden', k !== tab));
      if (scrollTo) {
        const t = document.getElementById(scrollTo);
        if (t) t.scrollIntoView({ block: 'start' });
      }
    }
    btns.forEach((b) => b.addEventListener('click', () => select((b as HTMLElement).dataset.tab!)));
    function route() {
      const h = location.hash.replace('#', '');
      if (h === 'akiya') select('akiya', 'akiya');
      else if (h === 'recruitment' || h === 'consulting') select('shien', h);
    }
    route();
    window.addEventListener('hashchange', route);
  </script>
</Layout>
