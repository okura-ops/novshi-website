---
interface TimelineItem {
  period: string;
  company: string;
  role: string;
  detail?: string;
}

interface Props {
  items: TimelineItem[];
}

const { items } = Astro.props;
---

<div class="relative">
  <div class="absolute left-4 lg:left-6 top-0 bottom-0 w-px bg-border"></div>

  <div class="space-y-10">
    {items.map((item, index) => (
      <div class="relative pl-12 lg:pl-16 fade-in-up">
        <div class:list={[
          'absolute left-2.5 lg:left-4.5 top-1 w-3 h-3 rounded-full border-2 border-white',
          index === items.length - 1 ? 'bg-accent' : 'bg-navy-200',
        ]}></div>

        <div class="text-xs font-semibold text-accent tracking-wider uppercase mb-1">{item.period}</div>
        <h3 class="text-lg font-bold text-gray-900">{item.company}</h3>
        <p class="text-sm text-text-secondary mt-1">{item.role}</p>
        {item.detail && (
          <p class="text-xs text-text-muted mt-1">{item.detail}</p>
        )}
      </div>
    ))}
  </div>
</div>
